import { CapMoreThan4Pipe } from './cap-more-than4.pipe';

describe('CapMoreThan4Pipe', () => {
  it('create an instance', () => {
    const pipe = new CapMoreThan4Pipe();
    expect(pipe).toBeTruthy();
  });
});
