import { MultiplyerPipe } from './multiplyer.pipe';

describe('MultiplyerPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplyerPipe();
    expect(pipe).toBeTruthy();
  });
});
